# 🎯 Git部署 - 操作卡片

## 你的GitHub仓库
**https://github.com/newbee408/chat-app**

---

## ✅ 已完成
- ✅ Git仓库已初始化
- ✅ 代码已提交
- ✅ 远程仓库已配置

---

## 📋 接下来做这3件事

### 1️⃣ 在GitHub创建仓库（2分钟）

访问: **https://github.com/new**

填写：
- Repository name: `chat-app`
- 选择: **Public**
- ⚠️ 不要勾选任何初始化选项

点击 **Create repository**

---

### 2️⃣ 推送代码到GitHub（1分钟）

**方法A: 双击运行脚本（最简单）**
```
双击桌面上的: push-to-github.ps1
```

**方法B: 手动执行命令**
```powershell
cd "$HOME\Desktop\chat-app"
git push -u origin main
```

如果要求登录，使用：
- 用户名: `newbee408`
- 密码: Personal Access Token (从 https://github.com/settings/tokens 获取)

---

### 3️⃣ 在服务器部署（5分钟）

#### 3.1 登录云服务商Web终端
- 阿里云: 控制台 → ECS → 远程连接
- 腾讯云: 控制台 → 云服务器 → 登录
- AWS: EC2 → Connect

#### 3.2 复制粘贴这一条命令（一键部署）

```bash
cd /root && git clone https://github.com/newbee408/chat-app.git && cd chat-app && npm install --production && npm install -g pm2 && pm2 start ecosystem.config.json && pm2 save && pm2 startup && sudo ufw allow 8080/tcp && echo "" && echo "✅ 部署完成！访问: http://38.123.103.120:8080" && pm2 status
```

#### 3.3 执行pm2 startup输出的命令
会看到类似这样的输出：
```bash
sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u root --hp /root
```
复制并执行它。

#### 3.4 配置云服务商安全组
**重要！** 在云服务商控制台开放 **TCP 8080** 端口

---

## 🌐 访问地址
**http://38.123.103.120:8080**

---

## 🔍 检查命令

在服务器上执行：
```bash
pm2 status              # 应显示 online
pm2 logs chat-app       # 查看日志
netstat -tlnp | grep 8080  # 检查端口
```

---

## 🔄 以后更新代码

**本地修改后:**
```powershell
cd "$HOME\Desktop\chat-app"
git add .
git commit -m "更新说明"
git push
```

**服务器更新:**
```bash
cd /root/chat-app
git pull
pm2 restart chat-app
```

---

## 📞 遇到问题？

查看详细文档：
- `执行步骤.md` - 完整操作指南
- `Git部署方案.md` - Git部署详解

---

**准备好了吗？开始第1步！** 🚀
