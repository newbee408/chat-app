# ⚡ 快速部署参考卡

## 🎯 目标
将聊天APP部署到: **38.123.103.120:8080**

---

## 📋 最快部署方法（3步）

### 第1步：上传文件
使用 **WinSCP** 上传 chat-app 文件夹到服务器 `/root/`

### 第2步：SSH执行命令
```bash
ssh root@38.123.103.120

cd /root/chat-app && npm install --production && npm install -g pm2 && pm2 start ecosystem.config.json && pm2 save && sudo ufw allow 8080/tcp
```

### 第3步：配置云服务商安全组
在云服务商控制台开放 **TCP 8080** 端口

---

## 🌐 访问地址
http://38.123.103.120:8080

---

## 🔍 检查命令

```bash
# 查看应用状态
pm2 status

# 查看日志
pm2 logs chat-app

# 重启应用
pm2 restart chat-app
```

---

## 📁 服务器文件位置
```
/root/chat-app/
├── server.js
├── package.json
├── ecosystem.config.json
└── public/
    ├── index.html
    ├── style.css
    └── app.js
```

---

## ⚠️ 必做检查项

- [ ] SSH能连接到 38.123.103.120
- [ ] 文件已上传到 /root/chat-app
- [ ] npm install 成功
- [ ] pm2 status 显示 online
- [ ] 云服务商安全组开放 8080
- [ ] 服务器防火墙允许 8080
- [ ] 浏览器能访问

---

## 🆘 快速修复

**无法访问？**
1. `pm2 logs chat-app` 查看错误
2. 检查云服务商安全组
3. `sudo ufw allow 8080/tcp`

**PM2不存在？**
```bash
npm install -g pm2
```

**端口被占用？**
```bash
sudo lsof -i :8080
sudo kill -9 <PID>
```

---

## 📞 文档位置

详细文档在本地：
- `部署指南.md` - 完整部署教程
- `MANUAL_DEPLOY.md` - 手动部署步骤
- `DEPLOY.md` - 技术文档
- `deploy.ps1` - Windows自动部署脚本
- `deploy.sh` - Linux/Mac自动部署脚本
